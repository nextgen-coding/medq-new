# Emergency single-item mode for QCM processing
# Copy these to your .env.local and restart dev server

# Force single-item processing (bypass batching)
AI_SINGLE_ITEM_MODE=1

# Conservative token limits
AZURE_MAX_TOKENS=8000
AI_IMPORT_MAX_TOKENS=8000

# Reduce timeouts to fail fast on problematic requests
AZURE_HTTP_TIMEOUT_MS=60000
AZURE_HTTP_RETRIES=2

# Optional: force small batches if single-item doesn't work
AI_BATCH_SIZE=1
AI_CONCURRENCY=1